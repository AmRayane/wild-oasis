import styled from "styled-components";

const StyleadHeader = styled.header`
  background-color: var(--color-grey-0);
`;

export default function Header() {
  return <StyleadHeader>Header</StyleadHeader>;
}
